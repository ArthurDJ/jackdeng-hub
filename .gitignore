node_modules
.next
.env
.env.local
.env.*.local
.env.development
.env.production
dev.log
tunnel-temp.log

# Logs and runtime artifacts
*.log
server*.log

# Claude/Agent specific context
.claude/

# Temporary or scripts to be ignored
check-slugs.ts

.vercel

# Local DB snapshots (contain production data — never commit)
backups/

# OS cruft
.DS_Store
**/.DS_Store

# TypeScript incremental build cache
*.tsbuildinfo

# Payload local upload dir — production uploads go to Vercel Blob.
# Keep the directory, ignore whatever dev writes into it.
public/media/*
!public/media/.gitkeep
